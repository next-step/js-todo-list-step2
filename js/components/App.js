import UserTitle from "./UserTitle.js";
import { DEFAULT_USER } from "../constants.js";

class App {
  constructor($target) {
    this.$target = $target;
    this.state = { username };
    this.initComponents();
  }

  render() {}

  initComponents() {}
}

export default App;
