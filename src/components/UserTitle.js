import { Component } from "../core/Component.js";

export const UserTitle = class extends Component {

  $state; $target;

  constructor(target) {
    super(target);
  }

  render() {
    return ``;
  }

}