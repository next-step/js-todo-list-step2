import api from '../api/api.js';

class UserList {
  constructor() {
    this.$userList = document.querySelector('#user-list');
    this.$userCreateButton = document.querySelector('button.user-create-button');
  }

  addUser(name) {}
}
