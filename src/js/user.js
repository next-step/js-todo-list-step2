const userList = [];
const user = (() => {
    const addUser = (name) => {
        const newUser = (name) => {
            name: name
        }
        return newUser(name);
    };

    return {
        addUser
    };

})();

export default user


